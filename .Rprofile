source("renv/activate.R")

# Allow access to user library for development tools (languageserver, etc.)
# This prevents VS Code from repeatedly prompting to install languageserver
.libPaths(c(.libPaths(), .Library.site, .Library))
